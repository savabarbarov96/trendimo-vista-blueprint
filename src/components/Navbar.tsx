import React from "react";

// This component has been replaced by EnhancedTubelightNavbar
// It's now an empty component to prevent breaking existing imports
const Navbar = () => {
  return null;
};

export default Navbar;
